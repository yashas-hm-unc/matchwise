import 'package:flutter/material.dart';

class ForceMatchPage extends StatelessWidget {
  const ForceMatchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
